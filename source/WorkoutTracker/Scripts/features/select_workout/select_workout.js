(function(views, util) {
    views.select_workout = React.createClass({
        render: function() {
            return (
              <div className="commentBox">
                Please select a workout
              </div>
          );
        }
    });
}(app.views, app.util));